# Fish private variables (machine-specific runtime state)
fish/.config/fish/fish_variables

# Neovim lazy.nvim internal git dir (not a real submodule)
nvim/.config/nvim/.git

# Runtime theme pointer (updated by switch-theme at runtime)
tmux/.config/tmux/theme.conf
